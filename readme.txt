Install python 3.7.0 or higher:
$ sudo apt update
$ sudo apt install python3.7

Install python packages:
$ pip install opencv-python==4.0.0.21
$ pip install tk

Run the program:
$ cd path-to/mlep
$ python gui.py

Note: DO NOT DELETE the encode and decode directories.
